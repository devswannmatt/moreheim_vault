<div class="row">
    <div class="col s12 m12">
        <div class="card">
            <form id="roster-name-form" data-method="PATCH" action="/rosters/roster/{{roster._id}}">
                <div class="card-content">
                    <div class="row">
                        <div class="col s3">
                            <div
                                style="width:100%; height:140px; display:flex; align-items:center; justify-content:center; background:#e3f2fd;">
                                <i class="ra ra-double-team" style="font-size:28px; color:#1565c0"></i>
                            </div>
                            <div style="margin-top:8px; text-align:center">
                                <div class="grey-text text-darken-1">{{roster.player.name}}</div>
                            </div>
                        </div>
                        <div class="col s9">
                            <div style="flex-grow:1">
                                <span class="card-title" style="margin:0">
                                    <div class="input-field">
                                        <input id="name" name="name" type="text" value="{{roster.name}}" required>
                                        <label for="name" class="active">Name</label>
                                    </div>
                                </span>

                                <div class="input-field">
                                    <textarea id="description" name="description" class="materialize-textarea" rows="2"
                                        style="min-height:48px">{{roster.description}}</textarea>
                                    <label for="description" class="active">Description</label>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="row" style="margin-bottom:0">
                        <div class="col s6">
                            <div class="input-field">
                                <input id="rating" name="rating" type="number" value="{{roster.rating}}" required>
                                <label for="rating" class="active">
                                    <i class="ra ra-podium"></i> Rating
                                </label>
                            </div>
                        </div>
                        <div class="col s6">
                            <div class="input-field">
                                <input id="gold" name="gold" type="number" value="{{roster.gold}}" required>
                                <label for="gold" class="active">
                                    <i class="ra ra-gold-bar"></i> Gold
                                </label>
                            </div>
                        </div>
                    </div>
                    <span class="grey-text text-darken-1" style="margin-right:12px">
                        Last updated:
                        {{formatDateTime roster.updatedAt}}
                    </span>
                </div>
                <div class="card-action blue lighten-5">
                    <div class="row" style="margin-bottom:0">
                        <div class="col s6">
                            <a class="btn-small blue" href="/rosters/roster/{{roster._id}}/inventory">Inventory</a>
                            <a class="btn-small green" href="/members/member/create">Add Member</a>
                        </div>
                        <div class="col s6 right-align">
                            <div class="btn-area">
                                <button id="delete-roster-btn" type="button" class="btn waves-effect waves-light red"
                                    title="Delete roster">
                                    <i class="ra ra-tombstone"></i>
                                </button>
                                <button class="btn waves-effect waves-light" type="submit">
                                    <i class="ra ra-save"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </form>
        </div>
        <div class="card">
            <div class="card-content">
                <span class="card-title">Members</span>
                <table class="striped responsive-table" id="members-table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Unit</th>
                            <th>Quantity</th>
                            <th>M</th>
                            <th>WS</th>
                            <th>BS</th>
                            <th>S</th>
                            <th>T</th>
                            <th>W</th>
                            <th>I</th>
                            <th>A</th>
                            <th>LD</th>
                            <th>Rating</th>
                            <th>Gold</th>
                        </tr>
                    </thead>
                    <tbody>
                    {{#each members}}
                        <tr>
                            <td><a href="/members/member/{{this._id}}">{{this.name}}</a></td>
                            <td>{{this.unit.name}}</td>
                            <td>{{this.qty}}</td>
                            <td>{{this.unit.m}}</td>
                            <td>{{this.unit.ws}}</td>
                            <td>{{this.unit.bs}}</td>
                            <td>{{this.unit.s}}</td>
                            <td>{{this.unit.t}}</td>
                            <td>{{this.unit.w}}</td>
                            <td>{{this.unit.i}}</td>
                            <td>{{this.unit.a}}</td>
                            <td>{{this.unit.ld}}</td>
                            <td>{{this.rating}}</td>
                            <td>{{this.gold}}</td>
                        </tr>
                    {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>